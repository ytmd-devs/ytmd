import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.th_ch.youtube_music',
  appName: 'YouTube Music',
  webDir: 'dist/renderer'
};

export default config;
